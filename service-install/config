# （必须）应用服务名称，需要确保当前系统没有该同名服务
SERVICE_NAME=test2
# （必须）服务对应的安装程序文件名
SERVICE_FILE=test.jar
# （必须）服务程序的main方法类
MAIN_FUNCTION=com.test
# 服务描述
SERVICE_DESC=asd
# 服务版本，默认为1.0
VERSION=1.1.0
# 服务安装位置，默认为/opt/$SERVICE_NAME
INSTALL_PATH=
# JAVA环境变量
JAVA_HOME=/usr/lib/jvm/jre-11-openjdk-11.0.11.0.9-1.el7_9.x86_64
# JVM选项
JVM_OPTIONS=
# 程序需要的参数
PROGRAM_ARGUMENTS=
# 服务依赖的某些程序的路径，安装时需要一同安装，多个路径用“,”分隔
LIB_PATH=lib/test/*
# 服务安装后的管理命令，默认为$SERVICE_NAME
COMMEND_NAME=config
